/**
 * Defines if a chart is of type line, bar or scatter
 */
export interface NeChartType {
  /**
   * Type line
   */
  line: boolean;

  /**
   * Type bar
   */
  bar: boolean;

  /**
   * Type scatter
   */
  scatter: boolean;
}
